# Postgres connection string. A standard DSN; provisioned automatically at setup.
DATABASE_URL=

# REQUIRED external third-party API key. There is no default, and it cannot be
# provisioned locally — obtain it from the provider and supply it as a secret.
# The service refuses to start without it.
API_KEY=

# Optional; defaults to 8080.
PORT=8080
